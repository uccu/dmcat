<IfModule mod_rewrite.c>

    RewriteEngine On
    RewriteRule ^$ download/share_pc.html
    
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)/$ /$1 [L,R=301]

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    
    RewriteRule ^ index.php [L]

</IfModule>
